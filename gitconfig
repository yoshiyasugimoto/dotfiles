[ghq]
	root = ~
[includeIf "gitdir:~/Documents/project/my_project/**"]
    path = ~/.gitconfig_root
[includeIf "gitdir:~/Documents/project/work/**"]
    path = ~/.gitconfig_xxx

# delta settings
[core]
pager = delta
[interactive]
diffFilter = delta --color-only
[delta]
side-by-side = true # use splite view
line-numbers = true # show line number
navigate = true    # use n and N to move between diff sections
light = false
[merge]
conflictstyle = diff3
[diff]
colorMoved = default
